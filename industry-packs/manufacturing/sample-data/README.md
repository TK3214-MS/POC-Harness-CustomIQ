# Manufacturing sample data

All records are synthetic. Upload `fabric/quality_issues.csv` to Fabric Lakehouse and load it as a managed table for the `QualityIssue` entity type. Use `work-iq/` as optional workplace content to create manually in a test mailbox, team channel, or document library.

The existing `knowledge/` documents are the optional Foundry IQ document set.
