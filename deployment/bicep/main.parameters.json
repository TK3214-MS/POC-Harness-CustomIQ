{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "mcpBackendExists": {
      "value": "${SERVICE_MCP_BACKEND_RESOURCE_EXISTS}"
    },
    "industryPack": {
      "value": "${IIQ_INDUSTRY_PACK}"
    },
    "dataSeed": {
      "value": "${IIQ_DATA_SEED}"
    }
  }
}
