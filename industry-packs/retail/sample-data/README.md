# Retail sample data

All records are synthetic. Upload `fabric/inventory_records.csv` to Fabric Lakehouse and load it as a managed table for the `InventoryRecord` entity type. Use `work-iq/` as optional content in a dedicated test workplace account.

The existing `knowledge/` documents are the optional Foundry IQ document set.
