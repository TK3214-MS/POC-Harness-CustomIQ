# Microsoft Product Verification Tracker

Status: **Most items below remain unverified.** However, on 2026-09-10, a subset of rows (Work IQ, Foundry IQ, Fabric IQ, and MCP support) were verified against live Microsoft Learn documentation supplied by the user and fetched by GitHub Copilot in this session. See the "2026-09-10 verification" notes below each updated row for the source URLs and findings. **This was AI-assisted verification (an AI agent fetched and summarized the pages at the user's request), not a manual review by a named human expert.** A human with Microsoft Learn/product access should still spot-check these findings before treating them as final, especially before promoting any `config/capabilities.yaml` status to GA. Every other row is `TBD - VERIFY AGAINST CURRENT MICROSOFT DOCUMENTATION` until someone with access to current docs/tenant confirms it. This file is the single source of truth feeding `config/capabilities.yaml`; do not duplicate these facts elsewhere as if independently confirmed.

Do not treat anything in this file as fact beyond what is explicitly marked as verified with a source and date. It exists so that every place in the codebase that depends on a Microsoft product detail points here instead of re-guessing.

| Capability | What needs verification | Current status in repo | Verification method (when available) |
|---|---|---|---|
| Microsoft Copilot Studio — "GitHub Copilot harness" | Exact product name, GA/Preview status, licensing model, availability of "harness" concept as described in instructions | **Verified 2026-09-10 (round 2, [ADR-0016](0016-copilot-studio-github-harness-confirmed.md)).** Source: [Harnesses in Copilot Studio](https://learn.microsoft.com/en-us/microsoft-copilot-studio/harnesses-overview) (ms.date 2026-07-28). "GitHub Copilot harness" is a real, named harness/runtime in Microsoft Copilot Studio (one of three: GitHub Copilot harness, standard harness, Copilot chat harness). It is billed via Copilot Credits (usage-based) per [billing-credit-overview]. Foundry IQ ([foundry-iq-connect](https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/foundry-iq-connect), ms.date 2026-06-23) and Fabric IQ ([fabric-iq-connect](https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/fabric-iq-connect), ms.date 2026-07-29, explicitly "(preview)") both connect natively to GitHub Copilot harness agents as first-party Tools, configured entirely within Copilot Studio (not via any code this repo controls). **Not confirmed**: whether Work IQ has an equivalent first-party Copilot Studio integration - it does not appear in the [knowledge source list](https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/knowledge-sources-overview) or as a dedicated "connect" page (a guessed `work-iq-connect` URL returned 404). Work IQ does expose its own standard MCP server (see Work IQ row below), so it is plausible it could be added via Copilot Studio's generic "Add MCP server" flow, but this specific combination has not been documented or confirmed - **flagged for user confirmation, not assumed.** | [Harnesses in Copilot Studio](https://learn.microsoft.com/en-us/microsoft-copilot-studio/harnesses-overview), [Foundry IQ is now in Copilot Studio (blog)](https://techcommunity.microsoft.com/blog/azure-ai-foundry-blog/foundry-iq-is-now-in-copilot-studio-bring-your-enterprise-data-to-every-agent-co/4534635) |
| Work IQ | Product existence/name, GA/Preview status, supported connectors (Teams/Outlook/SharePoint/OneDrive), licensing, regional availability | **Partially verified 2026-09-10, updated 2026-09-10 round 3.** Source: [Work IQ MCP overview](https://learn.microsoft.com/en-us/microsoft-365/copilot/extensibility/work-iq/mcp/overview) (ms.date 2026-08-20). Confirmed: Work IQ is a real, documented product exposing a Microsoft 365 MCP server with 10 fixed tools (`fetch`, `create_entity`, `update_entity`, `delete_entity`, `do_action`, `call_function`, `ask`, `list_agents`, `get_schema`, `search_paths`) operating on relative resource paths (e.g. `fetch /me/messages`). Auth is Microsoft Entra ID; MCP clients discover auth config via the `/.well-known/oauth-protected-resource` endpoint on the MCP server, and a Work IQ service principal (fixed appId `fdcc1f02-fc51-4226-8753-f668596af7f7`) is auto-provisioned in the tenant on first use (or explicitly via `az ad sp create --id fdcc1f02-fc51-4226-8753-f668596af7f7` / a Graph Explorer POST to `/v1.0/servicePrincipals`, per [Enable your tenant for Work IQ](https://learn.microsoft.com/en-us/microsoft-365/copilot/extensibility/work-iq/enable-work-iq), ms.date 2026-06-16). **Copilot Studio path confirmed indirect, not first-party**: Work IQ has no dedicated Copilot Studio "connect" page (unlike Foundry IQ/Fabric IQ), but it IS a documented Azure AI Search knowledge source kind (`workIQ`, see [Create a Work IQ Knowledge Source](https://learn.microsoft.com/en-us/azure/search/agentic-knowledge-source-how-to-work-iq), ms.date 2026-08-17) - so it can be added inside a Foundry IQ knowledge base, which Copilot Studio DOES connect to natively. Whether Copilot Studio's native Foundry IQ tool correctly forwards the Work IQ-specific `x-ms-query-work-iq-source-authorization` header at query time is **unconfirmed** - flagged for live testing, not assumed. See [docs/setup/Production-SaaS-Environment-Setup-Guide.md](../setup/Production-SaaS-Environment-Setup-Guide.md) Part C. **Not confirmed**: explicit GA/Preview maturity banner, exact licensing/regional availability. | Microsoft 365 Copilot / Work IQ docs — [tool reference](https://learn.microsoft.com/en-us/microsoft-365/copilot/extensibility/work-iq/tool-reference) still needs a follow-up fetch |
| Foundry IQ | Product existence/name, relationship to Azure AI Search / Azure AI Foundry Knowledge, GA/Preview status, confidence score semantics | **Partially verified 2026-09-10.** Source: [Connect Agents to Foundry IQ Knowledge Bases](https://learn.microsoft.com/en-us/azure/foundry/agents/how-to/foundry-iq-connect?tabs=foundry%2Cpython) (ms.date 2026-08-07, fetched 2026-09-10). Confirmed: Foundry IQ knowledge bases are backed by **Azure AI Search** agentic-retrieval knowledge bases, connected to a Microsoft Foundry Agent Service agent via **MCP** at the endpoint pattern `{search_service_endpoint}/knowledgebases/{knowledge_base_name}/mcp?api-version=2026-08-01-preview`, exposing a single `knowledge_base_retrieve` MCP tool. Auth uses a Foundry project `RemoteTool` connection (`ProjectManagedIdentity` auth type, or per-request `x-ms-query-source-authorization` header for per-user permission enforcement). The article states some agentic-retrieval features are GA in the `2026-04-01` REST API, but the MCP connection flow documented here uses the `2026-08-01-preview` API version — so **the specific MCP integration mechanism is Preview, not GA**. No native "application_confidence" score field was found in the fetched content (this repo's `_application_confidence()` remains an accelerator-derived heuristic, not a Microsoft-provided score — unchanged). | [Connect Agents to Foundry IQ Knowledge Bases](https://learn.microsoft.com/en-us/azure/foundry/agents/how-to/foundry-iq-connect) — re-check when the 2026-08-01-preview API version reaches GA |
| Fabric IQ | Product existence/name, relationship to Microsoft Fabric Data Agents / OneLake, GA/Preview status, Ontology feature maturity | **Partially verified 2026-09-10.** Source: [Connect agents to Microsoft Fabric with Fabric IQ (preview)](https://learn.microsoft.com/en-us/azure/foundry/agents/how-to/tools/fabric-iq?pivots=python) (ms.date 2026-08-05, fetched 2026-09-10). Confirmed: Fabric IQ is a real Microsoft Fabric workload, explicitly labeled **"(preview)"**, unifying OneLake data behind three item types — Ontology, Fabric data agent, Power BI semantic model — each with its own **MCP endpoint** (`server_url` patterns documented, e.g. `https://api.fabric.microsoft.com/v1/mcp/workspaces/{workspaceId}/dataagents/{dataAgentId}/agent` for a data agent). Connected to a Microsoft Foundry agent as a `fabric_iq_preview` tool (directly, or via a "toolbox" that itself becomes a generic `mcp`-type tool). Auth is BYO Entra app or managed OAuth (delegated On-Behalf-Of) for Ontology/Power BI semantic model; the data agent MCP endpoint additionally accepts a direct user/service-principal bearer token (`https://api.fabric.microsoft.com/.default` scope) for direct MCP clients. | [Connect agents to Microsoft Fabric with Fabric IQ (preview)](https://learn.microsoft.com/en-us/azure/foundry/agents/how-to/tools/fabric-iq) — re-check when preview label is removed |
| MCP support in Copilot Studio / Foundry | Native MCP tool registration support, allowlisting mechanism, auth model | **Verified 2026-09-10 for both.** Microsoft Foundry Agent Service natively supports MCP tools (confirmed via the Foundry IQ / Fabric IQ Foundry Agent Service pages). **Microsoft Copilot Studio's GitHub Copilot harness also natively supports adding any MCP server as a Tool** via "Add a Model Context Protocol (MCP) server as a tool" ([tools-add-mcp-server](https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/tools-add-mcp-server), ms.date 2026-07-28): the maker supplies a Name, Description, Server URL (HTTPS endpoint), and an authentication method; Copilot Studio performs the MCP protocol handshake and lists the exposed tools. This confirms that this repo's MCP Backend (`services/mcp-backend/`) is the correct architectural shape for Copilot Studio integration, **provided it is upgraded to a real MCP-protocol-compliant server** (see [ADR-0016](0016-copilot-studio-github-harness-confirmed.md) - the current REST-shaped `/health`/`/tools`/`/tools/{name}/invoke` API is not itself MCP-protocol-compliant). Limits: MCP servers count against an agent's total tool count, and concurrent MCP server instances per conversation are capped. | [Add a Model Context Protocol (MCP) server to your agent as a tool (preview)](https://learn.microsoft.com/en-us/microsoft-copilot-studio/agents-experience/tools-add-mcp-server) |
| Microsoft Entra ID | Current recommended patterns for delegated vs. app-only auth for agent tool calls, On-Behalf-Of flow support for MCP tools | **Partially verified 2026-09-10.** All three fetched IQ-layer MCP integrations use Microsoft Entra ID, but the recommended auth pattern differs per capability and per Fabric item type: Work IQ MCP auto-discovers OAuth config per-client via `/.well-known/oauth-protected-resource`; Foundry IQ knowledge base MCP uses a Foundry project's managed identity (`ProjectManagedIdentity`) or per-request user tokens; Fabric IQ uses delegated On-Behalf-Of (BYO Entra app or managed OAuth) for Ontology/Power BI semantic models, plus an additional direct bearer-token path (app-only or user) for the Fabric data agent MCP endpoint specifically. This is materially different from this repo's current `LiveAdapterSettings`/`entra_auth.py` design, which assumes one shared `ClientSecretCredential` (client-credentials, app-only) flow for all three IQ layers — see [ADR-0015](0015-mcp-native-iq-layer-integration.md). | Microsoft Entra ID docs (OBO flow specifics not yet independently re-checked) |
| Microsoft Purview | Current sensitivity label / DLP integration points applicable to agent-retrieved content | TBD - VERIFY AGAINST CURRENT MICROSOFT DOCUMENTATION | Microsoft Purview docs |
| Licensing/pricing for all of the above | Any $ or license-tier figures | TBD - VERIFY AGAINST CURRENT MICROSOFT DOCUMENTATION | Official pricing pages at time of verification |
| Regional availability for all of the above | Region lists | TBD - VERIFY AGAINST CURRENT MICROSOFT DOCUMENTATION | Product region availability pages |
| Preview vs. GA status for all of the above | Feature maturity | Fabric IQ and the Foundry IQ MCP connection flow are confirmed **Preview** as of 2026-09-10 (see rows above); Work IQ MCP maturity is still unconfirmed; Copilot Studio remains fully TBD | Release notes / product docs |

## Process

1. Every `capability_id` in `config/capabilities.yaml` must have a matching row here.
2. `last_verified_date` in the registry may only be set to a real date once a named person has checked a named, dated Microsoft source — never inferred from training data or general familiarity. AI-assisted verification (as done on 2026-09-10 above) is a starting point, not a substitute for this — a human should still confirm before the registry status is promoted to `GA`.
3. Until verified, `status` in the registry stays `Unknown` or `Verify Before Use`, and any customer-facing text referencing the capability must include the literal string `TBD - VERIFY AGAINST CURRENT MICROSOFT DOCUMENTATION`.
4. When a verification is performed, update this table with: verifier name, date, source URL/doc title, and the resulting status — then update the registry in the same change.
